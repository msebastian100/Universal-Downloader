# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv
.venv_build_mac

# Launcher (lokal behalten, nicht ins Repository)
start_launcher.bat
start_launcher.sh
start_launcher.vbs

# Build-Skripte (lokal behalten, nicht ins Repository)
build_apt_repo.sh
build_flatpak.sh
build_installer.bat
build_installer.py
build_linux.sh
build_mac.sh
build_msix.ps1
build_windows.py
build_zip_windows.py
zip_for_linux.sh
zip_for_windows.sh

# Build-Artefakte
build/
dist/
*.exe
*.deb
*.zip
*.snap
.flatpak-builder/
flatpak-build/
*.flatpak
parts/
stage/
prime/
.snapcraft/
.deps_ok
# UniversalDownloader.spec sollte im Repo sein (nicht ignorieren)

# GPG-Schlüssel (niemals committen)
.gnupg/
.gnupg.zip

# IDE
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# Logs und lokale Laufzeitdateien (nie in Releases)
Logs/
logs
*.log
*.log.txt
vbs.log.txt
.ytdlp_update_check.json

# Cursor (lokal)
.cursor/

# Konfiguration (sensible Daten)
.deezer_config.json
.audible_config.json
settings.json
.ftp-credentials

# OS
.DS_Store
Thumbs.db
*.tmp

# Downloads (optional - falls nicht ins Repository soll)
# Downloads/

# Audible Activator (separates Repository)
audible-activator/

# Dokumentation (lokal behalten, aber nicht ins Repository)
*.md
# README.md soll im Repository sein
!README.md